// "use client";

// import { StarIcon } from "@radix-ui/react-icons";
// import { useUserAccessPlan } from "app/(ai-payments)/(frontend)/contexts/user-access-plan-context";

// export function CreditBalance() {
//   const { credits, isLoading } = useUserAccessPlan();
//   return (
//     <div className="text-primary">
//       {isLoading ? <StarIcon className="mb-1 inline h-4 w-4 animate-spin" /> : credits} Credit{credits != 1 ? "s" : ""}
//     </div>
//   );
// }
