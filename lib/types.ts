export type ArchetypeValues = {
  explorer: string;
  analyst: string;
  designer: string;
  optimizer: string;
  connector: string;
  nurturer: string;
  energizer: string;
  achiever: string;
};

export type ReportType = string | undefined;

export type UserProfile = {
  name: string;
  email: string;
  address?: string;
  phone?: string;
};
